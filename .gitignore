# Dependencies
/target/

# Secrets and local config — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
*.p12
tokens.json
**/tokens.json
.schwabinvestbot/
safety.json

# OS / editors
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs and temp
*.log
*.rs.bk

# Plan runtime state (future)
plans/*.state.json
plans/local/

# Options agent runtime state (per rules file: agent-state-<rules-stem>.json)
rules/agent-state-*.json
rules/agent-*.pid
rules/agent-*.log
# Equity trader runtime state (per rules file)
rules/trader-state-*.json
rules/trader-journal-*.jsonl
# Legacy shared paths (pre per-file state)
rules/agent-state.json
rules/agent.pid

# Cursor local IDE config
.cursor/
