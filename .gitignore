# Dependencies
/target/

# Secrets and local config — NEVER commit
.env
.env.*
!.env.example
*.pem
*.key
*.p12
tokens.json
**/tokens.json
.schwabinvestbot/
safety.json

# OS / editors
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Logs and temp
*.log
*.rs.bk

# Plan runtime state (future)
plans/*.state.json
plans/local/

# Options agent runtime state (per rules file stem, e.g. options-pilot-9947.yaml)
rules/agent-state-*.json
rules/agent-sim-state-*.json
rules/agent-journal-*.jsonl
rules/agent-sim-journal-*.jsonl
rules/agent-*.pid
rules/agent-*.log
rules/.eqo-market-hours-*.json

# Equity trader runtime state (per rules file stem, e.g. trader-swing-9947.yaml)
rules/trader-state-*.json
rules/trader-journal-*.jsonl
rules/trader-*.pid
rules/trader-*.log

# Backtest artifacts (regenerate: schwab-trader / schwab agent backtest prefetch / run)
rules/analysis-*/.backtest-cache-*.json
rules/.backtest-cache-*.json
rules/.options-backtest-cache-*.json
rules/trader-backtest-state-*.json
rules/trader-backtest-journal-*.jsonl
rules/agent-backtest-state-*.json
rules/agent-backtest-journal-*.jsonl
rules/prefetch-*.sh
rules/universe/fmp-discovered.yaml

# Local rule tuning / comparison copies (committed examples: *-example.yaml)
rules/trader-swing-p1-*.yaml
rules/trader-swing-*-baseline.yaml
rules/p1-sizing-results.json
rules/measure-frozen.yaml

# Personal agent rules (account-specific hashes — copy from *-example.yaml locally)
rules/*-8709.yaml
rules/*-9947.yaml
rules/options-monthly-income.yaml

# Personal project notes and launch scripts (not for the public repo)
reminders.md
option-start.sh
trader-sim-start.sh

# Legacy shared paths (pre per-file state)
rules/agent-state.json
rules/agent.pid

# Cursor local IDE config
.cursor/
