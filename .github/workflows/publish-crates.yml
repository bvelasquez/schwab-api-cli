name: Publish crates.io

on:
  push:
    branches:
      - main
    paths:
      - "crates/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "scripts/publish-crate.sh"
      - ".github/workflows/publish-crates.yml"

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Publish schwab-api
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: bash scripts/publish-crate.sh schwab-api

      - name: Wait for schwab-api index
        run: bash scripts/wait-for-crate.sh schwab-api

      - name: Publish schwab-market-data
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: bash scripts/publish-crate.sh schwab-market-data

      - name: Wait for schwab-market-data index
        run: bash scripts/wait-for-crate.sh schwab-market-data

      - name: Publish schwab-cli
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: bash scripts/publish-crate.sh schwab-cli
